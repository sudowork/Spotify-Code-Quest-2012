/* Spotify Code Quest 2012
 * @author Kevin Gao
 */

#include <iostream>

using namespace std;		// Because I'm lazy

int main() {
	// Make io faster
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	// Main
	int a,b;
	while (cin >> a >> b) {
	}
	
	
	return 0;
}
